{
  "name": "{{ packageId }}",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint . --ext .js,.ts",
    "fix": "eslint . --ext .js,.ts --fix",
    "dev": "tsc --watch",
    "start": "node dist",
    "clean": "rm -rf dist/",
    "build": "yarn build:tsc",
    "build:tsc": "tsc"
  },
  "dependencies": {
    "tslib": "{{ tslibVersion }}"
  },
  "devDependencies": {
    "@hubvisor/build-core": "{{ buildCoreVersion }}"
  }
}
